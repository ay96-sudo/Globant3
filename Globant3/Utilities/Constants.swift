import Foundation
import CoreGraphics

struct Constants {
    enum ImageNames: String {
        case defaultImage = "default_image"
    }
}

enum Metrics {
    static let imageBottomConstraint: CGFloat = 15.0
    static let verticalStackViewConstraint: CGFloat = 0
    static let imageViewWidth: CGFloat = 130.0
}

